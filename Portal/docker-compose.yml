version: '3.8'

services:
  portal:
    build: .
    container_name: web-portal
    ports:
      - "8082:80"
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost/" ]
      interval: 30s
      timeout: 10s
      retries: 3
